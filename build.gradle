plugins {
    id 'java-library'
    id 'maven-publish'
    id 'net.neoforged.moddev' version '2.0.141'
    id 'idea'
}

tasks.named('wrapper', Wrapper).configure {
    // Define wrapper values here so as to not have to always do so when updating gradlew.properties.
    // Switching this to Wrapper.DistributionType.ALL will download the full gradle sources that comes with
    // documentation attached on cursor hover of gradle classes and methods. However, this comes with increased
    // file size for Gradle. If you do switch this to ALL, run the Gradle wrapper task twice afterwards.
    // (Verify by checking gradle/wrapper/gradle-wrapper.properties to see if distributionUrl now points to `-all`)
    distributionType = Wrapper.DistributionType.BIN
}

version = mod_version
group = mod_group_id

repositories
        {
            //emi
            maven {
                name = "TerraformersMC"
                url = "https://maven.terraformersmc.com/"
            }

            //jei
            maven {
                url = "https://dvs1.progwml6.com/files/maven/"
            }

            //jei
            maven {
                url = "https://modmaven.dev"
            }

            //favouriteless
            maven {
                url = "https://maven.favouriteless.net/releases"
            }

            //curios
            maven {
                name = "Illusive Soulworks maven"
                url = "https://maven.theillusivec4.top/"
            }

            //modrinth
            maven {
                url = "https://api.modrinth.com/maven"
            }

            //tyson the ember
            repositories {
                maven { url = 'https://maven.tysontheember.dev' }
            }

            repositories {
                exclusiveContent {
                    forRepository {
                        maven {
                            url "https://cursemaven.com"
                        }
                    }
                    filter {
                        includeGroup "curse.maven"
                    }
                }
            }
        }

dependencies {
    //libtooltips
    //jarJar(implementation("maven.modrinth:libtooltips:5J15zxaI"))

    //selling bin
    jarJar(implementation("maven.modrinth:wds-selling-bin:D66oRVjS"))

    //tiny multiblock lib
    jarJar(implementation("maven.modrinth:tiny-multiblock-lib:neo-26.1-${tiny_multiblock_lib_version}"))

    jarJar(implementation(files(".compatmods/emberstextapi-neoforge-26.1-3.0.0-devbuild.jar")))


    //EMI
    //compileOnly "dev.emi:emi-neoforge:${emi_version}:api"
    //runtimeOnly "dev.emi:emi-neoforge:${emi_version}"

    //JEI
    compileOnly "mezz.jei:jei-${mc_version}-neoforge:${jei_version}"
    runtimeOnly "mezz.jei:jei-${mc_version}-neoforge:${jei_version}"

    //Serene Seasons
    compileOnly "maven.modrinth:serene-seasons:ZwWxXUVt"
    //compileOnly files(".compatmods/SereneSeasons-neoforge-1.21.1-10.1.0.3.jar")
    //compileOnly files(".compatmods/GlitchCore-neoforge-1.21.1-2.1.0.0.jar")
    //runtimeOnly files(".compatmods/SereneSeasons-neoforge-1.21.1-10.1.0.3.jar")
    //runtimeOnly files(".compatmods/GlitchCore-neoforge-1.21.1-2.1.0.0.jar")

    //Ecliptic Seasons
    compileOnly "maven.modrinth:ecliptic-seasons:S7XmJvyQ"
    //compileOnly files(".compatmods/EclipticSeasons-1.21.1-neoforge-0.12.5-all.jar")
    //runtimeOnly files(".compatmods/EclipticSeasons-1.21.1-neoforge-0.12.5-all.jar")

    //terrafirmacraft for seasons
    //compileOnly files(".compatmods/Patchouli-1.21.1-92-NEOFORGE.jar")
    //compileOnly files(".compatmods/TerraFirmaCraft-NeoForge-1.21.1-4.0.13-beta.jar")
    //runtimeOnly files(".compatmods/Patchouli-1.21.1-92-NEOFORGE.jar")
    //runtimeOnly files(".compatmods/TerraFirmaCraft-NeoForge-1.21.1-4.0.13-beta.jar")

    //distant horizons
    //compileOnly files(".compatmods/DistantHorizons-2.3.6-b-1.21.1-fabric-neoforge.jar")
    //runtimeOnly files(".compatmods/DistantHorizons-2.3.6-b-1.21.1-fabric-neoforge.jar")

    //ftb teams
    compileOnly files(".compatmods/ftb-teams-neoforge-26.1.2.2.jar")
    //compileOnly files(".compatmods/ftb-library-neoforge-2101.1.28.jar")
    //compileOnly files(".compatmods/architectury-13.0.8-neoforge.jar")

    //runtimeOnly files(".compatmods/ftb-teams-neoforge-2101.1.7.jar")
    //runtimeOnly files(".compatmods/ftb-library-neoforge-2101.1.28.jar")
    //runtimeOnly files(".compatmods/architectury-13.0.8-neoforge.jar")

    //quality foods compat
    //compileOnly("maven.modrinth:quality-food:vLFBpk1n")
    //runtimeOnly("maven.modrinth:quality-food:vLFBpk1n")

    //shaders
    compileOnly "maven.modrinth:iris:YEGDGnJM"
    //runtimeOnly "curse.maven:sodium-394468:6382651"
    //runtimeOnly "curse.maven:irisshaders-455508:6661598"

    //carry on
    //implementation "curse.maven:carry-on-274259:7393892"

    //Reliquified Artifacts (Angler's Hat compat)
    //compileOnly "maven.modrinth:reliquified-artifacts:y32QSEHL"
    //runtimeOnly "maven.modrinth:reliquified-artifacts:y32QSEHL"

    //artifacts (Angler's Hat compat)
    //compileOnly "maven.modrinth:artifacts:rGPw090r"
    //runtimeOnly "maven.modrinth:artifacts:rGPw090r"

    //relics (Angler's Hat compat)
    //compileOnly "maven.modrinth:relics-mod:QFPRw8EU"
    //runtimeOnly "maven.modrinth:relics-mod:QFPRw8EU"

    //curios (check modifiers in curio slots)
    //compileOnly "top.theillusivec4.curios:curios-neoforge:${curios_version}:api"
    //runtimeOnly "top.theillusivec4.curios:curios-neoforge:${curios_version}"

    //octolib because jarinjar must be illegal or something
    //compileOnly "maven.modrinth:octo-lib:AbiyvpxR"
    //runtimeOnly "maven.modrinth:octo-lib:AbiyvpxR"

    //architectury for the dependency of the dependency of the dependency of anglers hat
    //compileOnly "maven.modrinth:architectury-api:ZxYGwlk0"
    //runtimeOnly "maven.modrinth:architectury-api:ZxYGwlk0"

    //pipez
    //runtimeOnly "maven.modrinth:pipez:z20ZR1YE"
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            artifactId = base.archivesName.get()
            from components.java
        }
    }

    repositories {
        if (System.getenv("MAVEN_USER") == null || System.getenv("MAVEN_PASS") == null) {
            mavenLocal()
        } else maven {
            name = "wdReleases"
            url = "https://maven.favouriteless.net/releases"

            credentials {
                username = System.getenv("MAVEN_USER")
                password = System.getenv("MAVEN_PASS")
            }
        }
    }
}

base {
    archivesName = mod_id
}

java {
    toolchain.languageVersion = JavaLanguageVersion.of(25)
    withSourcesJar()
    withJavadocJar()
}

neoForge {
    // Specify the version of NeoForge to use.
    version = project.neo_version

    // This line is optional. Access Transformers are automatically detected
    // accessTransformers = project.files('src/main/resources/META-INF/accesstransformer.cfg')

    // Default run configurations.
    // These can be tweaked, removed, or duplicated as needed.
    runs {
        client {
            client()

            // Comma-separated list of namespaces to load gametests from. Empty = all namespaces.
            systemProperty 'neoforge.enabledGameTestNamespaces', project.mod_id
        }

        server {
            server()
            programArgument '--nogui'
            systemProperty 'neoforge.enabledGameTestNamespaces', project.mod_id
        }

        // This run config launches GameTestServer and runs all registered gametests, then exits.
        // By default, the server will crash when no gametests are provided.
        // The gametest system is also enabled by default for other run configs under the /test command.
        gameTestServer {
            type = "gameTestServer"
            systemProperty 'neoforge.enabledGameTestNamespaces', project.mod_id
        }

        data {
            clientData()

            // example of overriding the workingDirectory set in configureEach above, uncomment if you want to use it
            // gameDirectory = project.file('run-data')

            // Specify the modid for data generation, where to output the resulting resource, and where to look for existing resources.
            programArguments.addAll '--mod', project.mod_id, '--all', '--output', file('src/generated/resources/').getAbsolutePath(), '--existing', file('src/main/resources/').getAbsolutePath()
        }

        // applies to all the run configs above
        configureEach {
            // Recommended logging data for a userdev environment
            // The markers can be added/remove as needed separated by commas.
            // "SCAN": For mods scan.
            // "REGISTRIES": For firing of registry events.
            // "REGISTRYDUMP": For getting the contents of all registries.
            systemProperty 'forge.logging.markers', 'REGISTRIES'

            // Recommended logging level for the console
            // You can set various levels here.
            // Please read: https://stackoverflow.com/questions/2031163/when-to-use-the-different-log-levels
            logLevel = org.slf4j.event.Level.WARN
        }
    }

    mods {
        // define mod <-> source bindings
        // these are used to tell the game which sources are for which mod
        // multi mod projects should define one per mod
        "${mod_id}" {
            sourceSet(sourceSets.main)
        }
    }
}

// Include resources generated by data generators.
sourceSets.main.resources { srcDir 'src/generated/resources' }

// Sets up a dependency configuration called 'localRuntime'.
// This configuration should be used instead of 'runtimeOnly' to declare
// a dependency that will be present for runtime testing but that is
// "optional", meaning it will not be pulled by dependents of this mod.
configurations {
    runtimeClasspath.extendsFrom localRuntime
}


// This block of code expands all declared replace properties in the specified resource targets.
// A missing property will result in an error. Properties are expanded using ${} Groovy notation.
var generateModMetadata = tasks.register("generateModMetadata", ProcessResources) {
    var replaceProperties = [
            minecraft_version      : minecraft_version,
            minecraft_version_range: minecraft_version_range,
            neo_version            : neo_version,
            mod_id                 : mod_id,
            mod_name               : mod_name,
            mod_license            : mod_license,
            mod_version            : mod_version,
            mod_authors            : mod_authors,
            neo_version_range      : neo_version_range
    ]
    inputs.properties replaceProperties
    expand replaceProperties
    from "src/main/templates"
    into "build/generated/sources/modMetadata"
}
// Include the output of "generateModMetadata" as an input directory for the build
// this works with both building through Gradle and the IDE.
sourceSets.main.resources.srcDir generateModMetadata
// To avoid having to run "generateModMetadata" manually, make it run on every project reload
neoForge.ideSyncTask generateModMetadata


tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8' // Use the UTF-8 charset for Java compilation
}

// IDEA no longer automatically downloads sources/javadoc jars for dependencies, so we need to explicitly enable the behavior.
idea {
    module {
        downloadSources = true
        downloadJavadoc = true
    }
}
